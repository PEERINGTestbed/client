[project]
name = "measure-catchments"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "peering",
]

[tool.uv.sources]
peering = { path = "../../py-peering" }
