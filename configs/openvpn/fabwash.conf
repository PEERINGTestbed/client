# PEERING VPN client config file for fabwash
# This file is automatically generated, do NOT edit it manually

client
pull # accept configs pushed from server
explicit-exit-notify # notify server on exit

daemon fabwash
nobind

proto udp6
remote fabwash.atlas.peering.ee.columbia.edu
dev tap20

ca certs/ca.crt
cert certs/client.crt
key certs/client.key

setenv daemon_name fabwash
script-security 2
up scripts/openvpn/up.sh
down scripts/openvpn/down.sh

management var/fabwash.sock unix
log var/fabwash.log
writepid var/fabwash.pid

# security
remote-cert-tls server
tls-client

# tls-exit prevents clients from reconnecting automatically.
# tls-exit # prevent zombie clients
